server:
  port: 3344

spring:
#  profiles: dev
  application:
    name:  microservicecloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:yanghuaqiang/cloudconfigtest.git #GitHub上面的git仓库名字